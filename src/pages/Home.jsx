import React from "react";

const HomePage = ({ type }) => {
  return (
    <div>
      <h1>Hello, {type}!</h1>
      <p>This is a simple React component</p>
    </div>
  );
};

export default HomePage;
